# Interface: IIdentityProfileRemoveRequest

Remove the profile for an identity.

## Properties

### pathParams?

> `optional` **pathParams**: `object`

The path parameters.

#### identity

> **identity**: `string`

The identity to remove the profile for.
